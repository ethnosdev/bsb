name: database_builder
description: A sample command-line application.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.13.0

dependencies:
  path: ^1.9.1
  sqlite3: ^3.5.2
  scripture: ^0.6.0
  # scripture:
  #   path: ../../scripture

dev_dependencies:
  lints: ^6.1.0
  test: ^1.24.0
